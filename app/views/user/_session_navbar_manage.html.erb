
<% if current_user %>

      <% if current_page?(posts_path) || current_page?(search_path)%>
        <!--<ul class></ul>-->
        <div class style="margin-right: 10rem; width: 65%">
          <!--id="searchPost"-->
          <%= render 'search/search_bar' %>
        </div>
      <% elsif current_page?(admin_manager_menu_path) || current_page?('/manage/add/new_user') %>
        <div class="pt-1">
          <h4 class style="padding-left: 13px">User Management</h4>
        </div>
      <% end %>

  <div class="d-flex" style="position: absolute; right: 0; top:3px">
    <a href="<%= edit_user_registration_path %>">
       <button type="button" class="btn btn-outline-primary rounded-start">
        <!--<div class="text-truncate">-->
          <small style="margin-right: 1rem; padding-top: 12px"> <%= current_user.first_name + " " + current_user.last_name %> </small>
          <!--<small> <br><%#= current_user.roles_name.first %> </small>-->
        <!--</div>-->
        <%= image_tag('profile.jpg', class: 'rounded-start', style: 'height:50px; width:50px') %>
       </button>
    </a>
  </div>

<% end %>

<!--<script>-->
<!--  var search_in_posts = () => {-->
<!--      var filter = document.getElementById('searchPost').value.toUpperCase();-->

<!--      var require_div = document.getElementById('allPosts');-->

<!--      var content = require_div.getElementsByTagName('p')-->

<!--      for ( var i=0; i < <%#= Post.all.count %>; i++) {-->
<!--          var inside = content[0].textContent || content[0].innerText;-->

<!--          if (inside.toUpperCase().indexOf(filter) > -1) {-->
<!--            content[0].style.display = '';-->
<!--          } else {-->
<!--              content[0].style.display = 'not found';-->
<!--          }-->
<!--      }-->
<!--  }-->
<!--</script>-->

    <!--<small>-->
      <%#=  current_user.roles  %>
    <!--</small>-->
